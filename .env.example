# Database (Docker Compose default)
DATABASE_URL=postgresql://resonance:resonance@localhost:5432/resonance_dev

# Auth.js v5 — generate with: npx auth secret
AUTH_SECRET=

# LLM / external services (added in later steps)
OPENAI_API_KEY=
FIRECRAWL_API_KEY=

# Docker Compose Postgres
POSTGRES_USER=resonance
POSTGRES_PASSWORD=resonance
POSTGRES_DB=resonance_dev
