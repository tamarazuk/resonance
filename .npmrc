# pnpm configuration
auto-install-peers=true

# Storybook dynamically imports framework/addon packages at runtime,
# which requires them to be hoisted so the CLI can resolve them.
public-hoist-pattern[]=@storybook/*
